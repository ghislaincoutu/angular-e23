import { Transform01Pipe } from './transform01.pipe';

describe('Transform01Pipe', () => {
  it('create an instance', () => {
    const pipe = new Transform01Pipe();
    expect(pipe).toBeTruthy();
  });
});
